import { DeviceRegistrationComponent } from './device-registration.component';

describe('DeviceRegistrationComponent', () => {
  it('creates component', () => {
    const component = new DeviceRegistrationComponent();
    expect(component).toBeTruthy();
  });
});
