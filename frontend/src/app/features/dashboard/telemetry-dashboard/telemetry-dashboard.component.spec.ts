import { TelemetryDashboardComponent } from './telemetry-dashboard.component';

describe('TelemetryDashboardComponent', () => {
  it('creates component', () => {
    const component = new TelemetryDashboardComponent();
    expect(component).toBeTruthy();
  });
});
