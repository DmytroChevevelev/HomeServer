// Application settings used by the client.
// Export a named constant so TypeScript treats this file as a module.

/**** Base URL of the WebAPI (no trailing slash) */
export const API_BASE = 'http://localhost:5043';
